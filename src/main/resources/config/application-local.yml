spring:
  datasource:
    username: postgres
    password: Pass1234
    url: jdbc:postgresql://localhost:5432/unitech
  jpa:
    hibernate:
      ddl-auto: update
    generate-ddl: true
    properties:
      dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  security:
    user:
      name: user
      password: password

server:
  port: 8090